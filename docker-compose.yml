prod:
  image: vizzuality/gfw-terrai-alerts-api:2.0.0${STAGING}
  ports:
    - "3600"
  env_file:
    - prod.env
  command: start
  log_driver: "gelf"
  restart: always
  log_opt:
    gelf-address: ${GELF_ADDRESS}
